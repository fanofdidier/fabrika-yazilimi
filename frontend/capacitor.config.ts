import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fabrikayazilimi.app',
  appName: 'Fabrika Yazılımı',
  webDir: 'build'
};

export default config;
